<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#16a34a">
    <link rel="manifest" href="/manifest.json">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <link rel="apple-touch-icon" href="/icons/icon-192.png">
    <title>@ViewData["Title"] - ODPO s.r.o.</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/Edit.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/Mobile.css" asp-append-version="true" />
    <!-- Standardní PWA Manifest -->
    <link rel="manifest" href="~/manifest.json">
    
    <!-- Barva lišty prohlížeče (pro Android) -->
    <meta name="theme-color" content="#16a34a">
    
    <!-- =============== -->
    <!-- iOS specifické tagy -->
    <!-- =============== -->
    
    <!-- Povolí běh v samostatném okně ("standalone" režim) -->
    <meta name="apple-mobile-web-app-capable" content="yes">
    
    <!-- Název aplikace pod ikonou na ploše -->
    <meta name="apple-mobile-web-app-title" content="ODPO Katalog">
    
    <!-- Styl stavového řádku (čas, baterie, signál) -->
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    
    <!-- Ikona pro plochu iOS -->
    <link rel="apple-touch-icon" href="/logo.png">
    
    @RenderSection("Styles", required: false)

    <link rel="apple-touch-icon" href="/logo.png">

    <style>
        :root {
            --brand: #16a34a;
            --brand-dark: #166534;
            --border: #dbe7d9;
            --bg: #f4fbf4;
            --text: #0b1f0b;
            --white: #fff;
            --pad: 12px;
            --gap: 8px;
            --radius: 12px;
        }

        * {
            box-sizing: border-box
        }

        body {
            margin: 0;
            font-family: system-ui,-apple-system,Segoe UI,Roboto,Ubuntu;
            color: var(--text);
            background: var(--bg)
        }

        header {
            position: sticky;
            top: 0;
            z-index: 5;
            background: #fff;
            border-bottom: 1px solid var(--border)
        }

        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 14px var(--pad)
        }

        .brand {
            display: flex;
            align-items: center;
            gap: 12px
        }

            .brand img {
                height: 36px;
                width: auto
            }

        h1 {
            margin: 0;
            font-size: 18px;
            font-weight: 700;
            color: var(--brand-dark)
        }

        .right {
            margin-left: auto;
            display: flex;
            gap: var(--gap);
            flex-wrap: wrap;
            align-items: center
        }
        /* ... případně další styly ... */
    </style>
</head>
<body>
    <header>

                <div class="right">
                    @if (User?.Identity?.IsAuthenticated ?? false)
                    {
                        <span>@User.Identity.Name</span>
                        <form asp-controller="Account" asp-action="Logout" method="post" style="display:inline;">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="pill">Odhlásit</button>
                        </form>
                    }
                    else
                    {
                        <a asp-controller="Account" asp-action="Login" class="pill">Přihlásit</a>
                        @* Registraci NEZOBRAZUJEME pokud nechcete veřejnou registraci.
                   Pokud chcete mít registraci přes vlastní UI, odkaz přidejte: *@
                        @* <a asp-controller="Account" asp-action="Register" class="pill">Registrovat</a> *@
                    }
                </div>



        <div class="container" style="display:flex;align-items:center;gap:12px">
            <div class="brand">
                <img src="/logo.png" alt="ODPO logo">
                <h1>ODPO s.r.o. – Katalog vzorků plastů</h1>
            </div>
            <div class="right">
                <a href="@Url.Action("Create", "Photos")" class="primary pill">+ Nový vzorek</a>
            </div>
        </div>
    </header>
    <main class="container">
        @RenderBody()
    </main>
    @RenderSection("Scripts", required: false)

<script>
if ('serviceWorker' in navigator) {
    window.addEventListener('load', () => {
        navigator.serviceWorker.register('/service-worker.js')
            .then(reg => console.log('Service Worker registered', reg))
            .catch(err => console.log('Service Worker registration failed', err));
    });
}
</script>


</body>
</html>