@model PhotoApp.Models.PhotoRecord

@{
    ViewData["Title"] = "Přidat nový záznam";
}


<style>
    :root {
        --brand: #16a34a;
        --brand-dark: #166534;
        --border: #dbe7d9;
        --bg: #f4fbf4;
        --text: #0b1f0b;
        --white: #fff;
        --pad: 12px;
        --gap: 8px;
        --radius: 12px;
        --bar-height: 108px;
    }

    * {
        box-sizing: border-box;
    }

    html {
        width: 100%;
        overflow-x: hidden;
    }

    body {
        margin: 0;
        padding: 0 0 108px 0;
        width: 100%;
        overflow-x: hidden;
        font-family: system-ui,-apple-system,Segoe UI,Roboto,Ubuntu;
        color: var(--text);
        background: var(--bg);
    }

    header {
        position: sticky;
        top: 0;
        z-index: 5;
        background: #fff;
        border-bottom: 1px solid var(--border);
        width: 100%;
    }

    .container {
        width: 100%;
        max-width: 1100px;
        margin: 0 auto;
        padding: 14px var(--pad);
        box-sizing: border-box;
    }

    .brand {
        display: flex;
        align-items: center;
        gap: 12px
    }

        .brand img {
            height: 36px;
            width: auto
        }

    h1 {
        margin: 0;
        font-size: 18px;
        font-weight: 700;
        color: var(--brand-dark);
    }

    .card, .center-form-container {
        background: #fff;
        border: 1px solid var(--border);
        border-radius: var(--radius);
        padding: 2rem 2.2rem 2rem 2.2rem;
        margin: 40px auto 0 auto;
        max-width: 720px;
        box-sizing: border-box;
        width: 100%;
        box-shadow: 0 2px 8px rgba(0,0,0,.08);
        display: flex;
        flex-direction: column;
        align-items: stretch;
    }

        .center-form-container h2 {
            margin-bottom: 1.8rem;
            text-align: center;
            font-size: 1.6rem;
            color: var(--brand-dark);
        }

    .styled-form {
        width: 100%;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 12px 18px;
        align-items: start;
        box-sizing: border-box;
    }

    .full {
        grid-column: 1 / -1;
    }

    .form-group {
        display: flex;
        flex-direction: column;
        margin-bottom: 0.5rem;
        width: 100%;
        box-sizing: border-box;
    }

        .form-group label {
            font-weight: 500;
            margin-bottom: 0.4rem;
            color: #333;
        }

    .form-control {
        padding: 0.5rem 0.9rem;
        border: 1px solid #cfd8dc;
        border-radius: 7px;
        font-size: 1rem;
        width: 100%;
        box-sizing: border-box;
    }

        .form-control:focus {
            outline: none;
            border-color: #0078d7;
        }

    .form-control-file {
        margin-top: 0.5rem;
        font-size: 1rem;
    }

    .btn-primary {
        background: var(--brand);
        color: #fff;
        padding: 0.7rem 1.6rem;
        border: none;
        border-radius: 8px;
        font-size: 1.1rem;
        font-weight: 500;
        cursor: pointer;
        transition: background 0.2s ease;
        margin-top: 0.2rem;
        box-shadow: 0 2px 8px rgba(0,0,0,0.03);
    }

        .btn-primary:hover {
            background: var(--brand-dark);
        }

    .btn {
        padding: 8px 16px;
        border-radius: 8px;
        border: 1px solid var(--border);
        background: var(--white);
        color: var(--brand-dark);
        cursor: pointer;
        font-weight: 500;
        margin-left: 10px;
        text-decoration: none;
        display: inline-block;
    }

        .btn:hover {
            background: #e6f9e9;
        }

    .preview-img {
        width: 180px;
        height: 180px;
        object-fit: cover;
        border-radius: 10px;
        border: 1px solid var(--border);
        margin-top: 10px;
    }

    .validation-summary {
        color: #b91c1c;
        margin-bottom: 8px;
        grid-column: 1 / -1;
    }

    .field-validation {
        color: #b91c1c;
        font-size: 0.95rem;
        margin-top: 6px;
    }

    .additional-preview {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        margin-top: 10px;
    }

        .additional-preview img {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border-radius: 6px;
            border: 1px solid var(--border);
        }

    /* Bottom action bar */
    .bottom-bar {
        position: fixed;
        left: 0;
        right: 0;
        bottom: 0;
        height: 108px;
        background: linear-gradient(180deg, var(--brand), var(--brand-dark));
        border-top: 1px solid rgba(0,0,0,0.06);
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 1500;
        padding: 10px;
        gap: 12px;
        box-shadow: 0 -6px 20px rgba(0,0,0,0.12);
        width: 100%;
        box-sizing: border-box;
    }

        .bottom-bar .actions-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 10px;
            width: 100%;
            max-width: 400px;
            padding: 6px;
            box-sizing: border-box;
        }

        .bottom-bar .action-btn {
            display: inline-flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 5px;
            padding: 8px;
            border-radius: 12px;
            text-decoration: none;
            color: #fff;
            background: rgba(255,255,255,0.05);
            border: 1px solid rgba(255,255,255,0.08);
            min-height: 60px;
            cursor: pointer;
        }

            .bottom-bar .action-btn img {
                width: 29px;
                height: 29px;
                display: block;
                filter: drop-shadow(0 2px 2px rgba(0,0,0,0.25));
            }

            .bottom-bar .action-btn .label {
                font-size: 8.5px;
                font-weight: 600;
                color: #fff;
                text-align: center;
            }

    /* Tablet - 768px and below */
    @@media (max-width: 768px) {
        .container {
            padding: 14px 12px;
        }

        .center-form-container {
            padding: 1.5rem 1.5rem 1.5rem 1.5rem;
            margin: 20px auto 0 auto;
        }

        .styled-form {
            grid-template-columns: 1fr;
            gap: 10px;
        }
    }

    /* Tablet - 640px and below */
    @@media (max-width: 640px) {
        body {
            padding: 0 0 96px 0;
        }

        :root {
            --bar-height: 96px;
        }

        .container {
            padding: 12px 10px;
        }

        .center-form-container {
            padding: 1.2rem 1rem 1.2rem 1rem;
            margin: 20px 8px 0 8px;
            border-radius: 10px;
        }

            .center-form-container h2 {
                margin-bottom: 1.2rem;
                font-size: 1.3rem;
            }

        .styled-form {
            grid-template-columns: 1fr;
            gap: 8px;
        }

        .preview-img {
            width: 140px;
            height: 140px;
        }

        .form-group {
            margin-bottom: 0.4rem;
        }

        .form-control {
            padding: 0.5rem 0.7rem;
            font-size: 16px;
        }

        textarea.form-control {
            min-height: 100px;
        }

        .btn, .btn-primary {
            padding: 0.6rem 1.2rem;
            font-size: 1rem;
        }

        .btn {
            margin-left: 8px;
        }

        h1 {
            font-size: 16px;
        }
    }

    /* Mobile - 420px and below */
    @@media (max-width: 420px) {
        body {
            padding: 0 0 100px 0;
        }

        .container {
            padding: 10px 8px;
        }

        header {
            border-bottom: 1px solid var(--border);
        }

        .center-form-container {
            padding: 1rem 0.8rem 1rem 0.8rem;
            margin: 16px 4px 0 4px;
            border-radius: 8px;
        }

            .center-form-container h2 {
                margin-bottom: 1rem;
                font-size: 1.2rem;
            }

        .page-header {
            flex-direction: column;
            align-items: flex-start !important;
        }

        .styled-form {
            grid-template-columns: 1fr;
            gap: 6px;
        }

        .form-group {
            margin-bottom: 0.3rem;
        }

            .form-group label {
                font-size: 0.9rem;
                margin-bottom: 0.3rem;
            }

        .form-control {
            padding: 0.5rem 0.6rem;
            font-size: 16px;
        }

        textarea.form-control {
            min-height: 80px;
        }

        .validation-summary {
            font-size: 0.9rem;
        }

        .field-validation {
            font-size: 0.85rem;
        }

        .preview-img {
            width: 120px;
            height: 120px;
        }

        .additional-preview img {
            width: 70px;
            height: 70px;
        }

        .btn-primary, .btn {
            padding: 0.5rem 1rem;
            font-size: 0.95rem;
        }

        .btn {
            margin-left: 6px;
        }

        .btn-primary:hover {
            background: var(--brand-dark);
        }

        .bottom-bar {
            height: 100px;
            padding: 8px;
            gap: 8px;
        }

            .bottom-bar .actions-grid {
                gap: 6px;
                padding: 4px;
            }

            .bottom-bar .action-btn {
                min-height: 48px;
                padding: 6px;
            }

                .bottom-bar .action-btn img {
                    width: 24px;
                    height: 24px;
                }

                .bottom-bar .action-btn .label {
                    font-size: 7px;
                }
    }

    /* Extra small - 360px */
    @@media (max-width: 360px) {
        .container {
            padding: 8px 6px;
        }

        .center-form-container {
            padding: 0.8rem 0.6rem 0.8rem 0.6rem;
            margin: 12px 2px 0 2px;
        }

        .form-control {
            padding: 0.4rem 0.5rem;
        }
    }
</style>

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <meta name="theme-color" content="#16a34a">
    <link rel="manifest" href="/manifest.json">
    <!-- Android / Chrome -->
    <meta name="mobile-web-app-capable" content="yes">
    <!-- iOS -->
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <link rel="apple-touch-icon" href="/icons/icon-192.png">
</head>

<header>
    <div class="container" style="display:flex;align-items:center;gap:12px">
        <div class="page-header" style="display:flex;align-items:center;gap:12px">
            <a asp-controller="Photos" asp-action="Index" style="display:inline-block;">
                <img src="@Url.Content("~/logo.png")" alt="ODPO logo" style="height:48px;width:auto;display:block;" />
            </a>
            <div>
                <h1 style="margin:0;font-size:18px;color:var(--brand);">Přidat nový záznam</h1>
                <div class="muted" style="font-size:13px;">ODPO s.r.o. – Katalog vzorků plastů</div>
            </div>
        </div>
    </div>
</header>

<main class="container">
    <div class="center-form-container">
        <h2>Přidat nový záznam</h2>

        <form id="createForm" asp-action="Create" method="post" enctype="multipart/form-data" class="styled-form" novalidate>
            @Html.AntiForgeryToken()

            <div asp-validation-summary="All" class="validation-summary"></div>

            <!-- left / right grid -->
            <div class="form-group">
                <label asp-for="Position">Pozice</label>
                <input asp-for="Position" class="form-control" />
                <span asp-validation-for="Position" class="field-validation"></span>
            </div>

            <div class="form-group">
                <label asp-for="ExternalId">ID (externí)</label>
                <input asp-for="ExternalId" class="form-control" />
                <span asp-validation-for="ExternalId" class="field-validation"></span>
            </div>

            <div class="form-group">
                <label asp-for="OriginalName">Původní název</label>
                <input asp-for="OriginalName" class="form-control" />
                <span asp-validation-for="OriginalName" class="field-validation"></span>
            </div>

            <div class="form-group">
                <label asp-for="Material">Materiál</label>
                <input asp-for="Material" class="form-control" />
                <span asp-validation-for="Material" class="field-validation"></span>
            </div>

            <div class="form-group">
                <label asp-for="Form">Forma</label>
                <input asp-for="Form" class="form-control" />
                <span asp-validation-for="Form" class="field-validation"></span>
            </div>

            <div class="form-group">
                <label asp-for="Filler">Plnivo</label>
                <input asp-for="Filler" class="form-control" />
                <span asp-validation-for="Filler" class="field-validation"></span>
            </div>

            <div class="form-group">
                <label asp-for="Color">Barva</label>
                <input asp-for="Color" class="form-control" />
                <span asp-validation-for="Color" class="field-validation"></span>
            </div>

            <div class="form-group">
                <label asp-for="MonthlyQuantity">Množství (měsíc(t))</label>
                <input asp-for="MonthlyQuantity" class="form-control" />
                <span asp-validation-for="MonthlyQuantity" class="field-validation"></span>
            </div>

            <div class="form-group">
                <label asp-for="Mfi">MFI</label>
                <input asp-for="Mfi" class="form-control" />
                <span asp-validation-for="Mfi" class="field-validation"></span>
            </div>

            <div class="form-group full">
                <label asp-for="Name">Název</label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="field-validation"></span>
            </div>

            <div class="form-group">
                <label asp-for="Supplier">Dodavatel</label>
                <input asp-for="Supplier" class="form-control" />
                <span asp-validation-for="Supplier" class="field-validation"></span>
            </div>

            <div class="form-group full">
                <label asp-for="Description">Popis</label>
                <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                <span asp-validation-for="Description" class="field-validation"></span>
            </div>

            <div class="form-group full">
                <label asp-for="Notes">Poznámky</label>
                <textarea asp-for="Notes" class="form-control" rows="3"></textarea>
                <span asp-validation-for="Notes" class="field-validation"></span>
            </div>

            <div class="form-group">
                <label for="PhotoFile">Hlavní fotka (soubor)</label>
                <input type="file" name="PhotoFile" id="PhotoFile" class="form-control-file" accept="image/*" />
                <span class="field-validation" id="photoError"></span>
            </div>

            <div class="form-group">
                <label for="PhotoPreview">Náhled hlavní fotky</label>
                <div>
                    <img id="PhotoPreview" src="" class="preview-img" alt="Náhled fotky" style="display:none;" />
                </div>
            </div>

            <div class="form-group full">
                <label for="AdditionalPhotoFiles">Dodatečné fotky (vyberte více najednou)</label>
                <input type="file" name="AdditionalPhotoFiles" id="AdditionalPhotoFiles" class="form-control-file" accept="image/*" multiple />
                <span class="field-validation" id="additionalPhotosError"></span>
                <div id="AdditionalPreview" class="additional-preview"></div>
            </div>

            <!-- actions -->
            <div class="form-group full" style="display:flex; gap:12px; justify-content:flex-end; margin-top: 18px;">
                <a href="@Url.Action("Index")" class="btn">Zpět</a>
                <button type="submit" class="btn-primary">Uložit</button>
            </div>
        </form>
    </div>
</main>

<!-- Bottom fixed action bar (2 tlačítka) -->
<nav class="bottom-bar" role="navigation" aria-label="Rychlé akce">
    <div class="actions-grid" role="menu" aria-label="Rychlé akce">
        <a role="menuitem" href="@Url.Action("Index", "Photos")" class="action-btn" title="Katalog">
            <img src="data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='48' height='48' viewBox='0 0 24 24' fill='white'><path d='M3 13h8V3H3v10zm0 8h8v-6H3v6zm10 0h8V11h-8v10zm0-18v6h8V3h-8z' fill='%23ffffff'/></svg>" alt="Katalog" />
            <div class="label">Katalog</div>
        </a>

        <a role="menuitem" href="#" class="action-btn" title="Uložit" onclick="document.getElementById('createForm').submit(); return false;">
            <img src="data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='48' height='48' viewBox='0 0 24 24' fill='white'><path d='M17 3H5c-1.11 0-2 .9-2 2v14c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V7l-4-4zm-5 16c-1.66 0-3-1.34-3-3s1.34-3 3-3 3 1.34 3 3-1.34 3-3 3zm3-10H5V5h10v4z' fill='%23ffffff'/></svg>" alt="Uložit" />
            <div class="label">Uložit</div>
        </a>
    </div>
</nav>

@section Scripts {
    <script>
        // Hlavní fotka - náhled
        document.getElementById('PhotoFile')?.addEventListener('change', function (e) {
            const input = e.target;
            const preview = document.getElementById('PhotoPreview');
            const err = document.getElementById('photoError');
            err.textContent = '';
            if (input.files && input.files[0]) {
                const file = input.files[0];
                if (!file.type.startsWith('image/')) {
                    err.textContent = 'Prosím vyberte obrázek.';
                    preview.style.display = 'none';
                    preview.src = '';
                    return;
                }
                const reader = new FileReader();
                reader.onload = function (ev) {
                    preview.src = ev.target.result;
                    preview.style.display = 'inline-block';
                };
                reader.readAsDataURL(file);
            } else {
                preview.style.display = 'none';
                preview.src = '';
            }
        });

        // Dodatečné fotky - náhled
        document.getElementById('AdditionalPhotoFiles')?.addEventListener('change', function (e) {
            const input = e.target;
            const previewContainer = document.getElementById('AdditionalPreview');
            const err = document.getElementById('additionalPhotosError');
            err.textContent = '';
            previewContainer.innerHTML = '';

            if (input.files && input.files.length > 0) {
                Array.from(input.files).forEach((file, index) => {
                    if (!file.type.startsWith('image/')) {
                        err.textContent = `Soubor ${file.name} není obrázek.`;
                        return;
                    }

                    const reader = new FileReader();
                    reader.onload = function (ev) {
                        const img = document.createElement('img');
                        img.src = ev.target.result;
                        img.alt = `Náhled dodatečné fotky ${index + 1}`;
                        previewContainer.appendChild(img);
                    };
                    reader.readAsDataURL(file);
                });
            }
        });
    </script>

    <partial name="_ValidationScriptsPartial" />
}